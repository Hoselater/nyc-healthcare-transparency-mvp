# Copy to .streamlit/secrets.toml for local runs, or paste the contents into
# Streamlit Community Cloud -> App settings -> Secrets.
#
# Optional. With no DATABASE_URL the app reads the committed CSV snapshot at
# data/nyc_ortho_scores_public.csv, which is what the public demo does.

# DATABASE_URL = "postgresql+psycopg2://user:password@host/dbname?sslmode=require"
